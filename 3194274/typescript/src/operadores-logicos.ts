/**
 * && = and
 * || = or
 * > = mayor que
 * < = menor que
 * >= = mayor o igual que
 * <= = menor o igual que
 *
 * ! = negación
 * != = diferencia de valor
 * == = comparación de valor
 *
 * === = comparación de valor y tipo
 * !== = diferencia de valor y tipo
 */

/* Razón de verdad
 * Todo valor de variable va a ser verdadero
 * si no es alguno de los siguientes:
 * - 0
 * - ""
 * - null
 * - undefined
 **/
const num = 1;
const name = "Alejo";
const car = {};

if (num) {
}

if (name) {
}

if (car) {
}
