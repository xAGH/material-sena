import { Highlighting } from './highlighting';

describe('Highlighting', () => {
  it('should create an instance', () => {
    const directive = new Highlighting();
    expect(directive).toBeTruthy();
  });
});
